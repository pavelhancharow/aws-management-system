import styled from 'styled-components';
import { FlexBoxCenterContainer } from '../shared/styles';

const AuthenticationWrapper = styled(FlexBoxCenterContainer)`
  width: 100%;
  height: 100vh;
`;

const AuthenticationStyles = {
  AuthenticationWrapper
}

export default AuthenticationStyles;
